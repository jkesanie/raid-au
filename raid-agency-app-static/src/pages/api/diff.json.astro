---
import diff from "@/temp/diff.json";
return new Response(JSON.stringify(diff), {
  headers: {
    "Content-Type": "application/json",
    "Access-Control-Allow-Origin": "*", // Allows access from any origin
  },
});
---
